# Design: one pool, two rails

Both rails show the **same** images. That is the product.

A “refs folder vs gens folder” split is artificial. You are picking two members of one set, not joining two datasets. Same files, two pickers. Click which one is the reference and which one is the candidate. Lock A, cycle B.

Do not “fix” this into separate lists.

The image set is the primary object. A and B are temporary selections from that set. Changing A or B never moves the camera or wipe.

Related: [README.md](./README.md)

Canonical: https://contraptions.bookofsarth.com/image-compare-workbench
Live: https://image-compare-workbench.marshy-runner.workers.dev
Shelf: https://contraptions.bookofsarth.com

A rated folder of gens, pairwise, is the job. JS-rendered paintings are gens in that folder. Same pool, two pickers. Do not split refs vs gens.
